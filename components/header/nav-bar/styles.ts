import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
  navBar: {
    display: 'flex',
  },
  navItem: {
    alignSelf: 'center',
  },
  closeIcon: {
    marginTop: '10px',
    marginLeft: '10px',
  },
})

export default useStyles
